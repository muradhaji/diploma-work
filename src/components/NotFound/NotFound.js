import React, { memo } from 'react';
// import PropTypes from 'prop-types';
// import styles from './NotFound.module.css';

const NotFound = (props) => {
  return <div>Axtardığınız səhifə tapılmadı :(</div>;
};

NotFound.propTypes = {};

export default memo(NotFound);
